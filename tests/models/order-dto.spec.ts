import { expect } from 'chai';

describe('OrderDTO', () => {
    describe('fromRequestBody', () => {
        it('Should return 3 when a = 1 and b = 2', () => {

            expect(5).to.equal(3);
        });
    });
});
